const browser = 'chrome';
const bowserMaximize = true;
const headless = '--headless';
const URL = 'https://facebook.com';
const chrome = require('selenium-webdriver/chrome');

module.exports = {
    browser,
    bowserMaximize,
    chrome,
    headless,
    URL
}

